import RuleEditor from '../src/RuleEditor'

describe('@icrules/editor', () => {

  const Editor = ({ facts, rules, onChange }) => {
    <RuleEditor {...{ facts, rules, onChange }} />
  }

  describe('baseline rule editing', () => {
    
  })

})